= design_box _("Enter details"), 500 do
  = simple_form_for [account, account_line] do |f|
    = f.input :text, autofocus: true
    = f.input :rent_at
    = f.input :booked_at
    = f.input :amount, input_html: {data: {autonumeric: true}}
    = f.submit _("Save")
