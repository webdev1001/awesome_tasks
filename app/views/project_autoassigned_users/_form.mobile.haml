= simple_form_for [project_autoassigned_user.project, project_autoassigned_user] do |f|
  = f.association :user, collection: users_collection, autofocus: true
  = f.submit t("save"), class: "mobile-button-primary"
