= design_box t("enter_criteria"), 350 do
  = simple_search_form_for ransack, ransack_params, remote: (params[:ajaxsearch] == "true"), html: {id: "form_users"}, data: {users_path: search_users_path} do |f|
    = hidden_field_tag :not_in_task_id, params[:not_in_task_id] if params[:not_in_task_id]
    = hidden_field_tag :not_in_project_id, params[:not_in_project_id] if params[:not_in_project_id]
    = hidden_field_tag :ajaxsearch, params[:ajaxsearch]
    = hidden_field_tag :jscallback, params[:jscallback]

    = f.input :name_cont

    = link_to t("add_new"), new_user_path, class: "button"
    = f.submit t("search")

- if params[:ajaxsearch] == "true"
  %br

  #divresults
    .error= t(".please_search")
