.task_progress{class: (task.task_checks.count == 0 ? "no-checks" : "has-checks")}
  .progress_bar{style: "width: #{(task.progress * 100).to_i}%;"}
  .progress_text
    = task.task_checks.count
    = " / "
    = task.task_checks.checked.count
